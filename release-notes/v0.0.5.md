# Well Level Bridge v0.0.5

Fixes filtered/raw odd behavior caused by outlier interval averages entering the long moving-average window.

The integration now range-checks and delta-checks each interval average before it is appended to the moving-average filter. Raw still shows the interval average, while filtered only advances from accepted interval averages.
