# Well Level Bridge v0.0.3

Improves frame parsing so the integration now looks for the numeric value immediately before the `Ft` unit first.

Token indexes remain available as a fallback, but normal frames no longer depend on the exact number of spaces between columns.
